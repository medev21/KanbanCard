
<p><%= link_to board.title, board %></p>
<%= link_to edit_board_path(board), remote: true do %>
    <button>Edit</button>
<% end %>

<%= link_to board_path(board), remote: true, method: :delete, data: {confirm: 'Are you sure'} do %>
    <button>Delete</button>
<% end %>
