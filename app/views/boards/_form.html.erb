<!-- <%= simple_form_for @board([@board, @boards.build]) do |f| %>
  <%= f.input  :title, label: 'Board' %>
  <%= f.button :submit %>
<% end %> -->

<%= simple_form_for @board, remote: true do |f| %>
  <%= f.input  :title, label: 'Board' %>
  <%= f.button :submit %>
<% end %>
